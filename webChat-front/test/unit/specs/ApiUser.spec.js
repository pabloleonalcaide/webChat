
describe('User API Service create user', () => {
  it('should send request if user field is present', () => {
    expect(null).toBeNull()
  })

  it('should return 201 if user doesnt exist yet', () => {
    expect(null).toBeNull()
  })
  it('should return 403 if user exists', () => {
    expect(null).toBeNull()
  })
})
